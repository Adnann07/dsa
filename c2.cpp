#include<bits/stdc++.h>
using namespace std;

int sumof(vector<int>& v,int n)
{
    if(n<=0) retrun 0;
    return (v[n-1],sumof(v,(n-1)));
}
int main()
{
    int n;
    cin>>n;
    for(int i=0;i<n;i++)
    {
        int x;
        cin>>x;
        v.push_back(x);
    }

    int sum(v,n);
}

